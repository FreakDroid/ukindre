import React from 'react';

//This will be handling the header side bar.
const Header = ({logo}) => {
    return (
        <header>
            <div id="logo">Unibet</div>
        </header>
    )
};

export default Header;